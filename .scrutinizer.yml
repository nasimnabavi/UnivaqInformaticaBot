build:
    tests:
        before:
            - pip3 install pylint
        override: 
            -
                command: 'pylint  ./ --output-format=text --reports=n --include-naming-hint=y  --disable=all'
                analysis:
                    file: 'pylint_result.json'
                    format: 'pylint-json'
