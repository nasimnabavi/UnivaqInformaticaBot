build:
    tests:
        before:
            - pip3 install pylint
        override: 
            -
                command: 'pylint  ./botcore.py  --output-format=text --reports=n --include-naming-hint=y > pylint_result.json'
                analysis:
                    file: 'pylint_result.json'
                    format: 'pylint-json'
