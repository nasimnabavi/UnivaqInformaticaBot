build:
    tests:
        before:
            - pip3 install pylint
        override: 
            -
                command: 'pylint  ./botcore.py  --output-format=ScrutinizerOutputFormatter --reports=n --include-naming-hint=y'
                analysis:
                    file: 'pylint_result.json'
                    format: 'pylint-json'
