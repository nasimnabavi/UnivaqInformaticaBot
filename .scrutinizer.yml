build:
    tests:
        before:
            - pip3 install pylint
        override: 
            -
                command: 'pylint  .  --output-format=parseable --reports=n --include-naming-hint=y > pylint_result.json'
                analysis:
                    file: 'pylint_result.json'
                    format: 'pylint-parseable'
